# ============================================================
# PASTE YOUR SUPABASE CREDENTIALS BELOW, THEN SAVE THIS FILE.
#
# This is the ONLY file you need to edit. You do not need to
# open, touch, or rename any .js or .html file anywhere in this
# project — the build process reads this file automatically and
# fills in these two values into all FOUR apps (Joseph Group
# Inspections, JGM, JPTS, and PTW).
#
# ONE DATABASE FOR EVERYTHING:
# Use your EXISTING PTWA Supabase project here (the one your live
# PTWA app already uses) — not a new one. All four apps will share
# this single database. PTWA keeps its existing permit tables and
# data exactly as they are; the other three apps get one new shared
# table added alongside them (see supabase_setup.sql).
#
# Where to find these two values:
#   1. Open your (existing PTWA) Supabase project in the browser
#   2. Click the gear icon (Settings) on the left sidebar
#   3. Click "API"
#   4. Copy "Project URL" and paste it after SUPABASE_URL=
#   5. Copy "anon public" key and paste it after SUPABASE_ANON_KEY=
#
# Do not add quote marks. Do not add spaces around the = sign.
# Just paste directly after the =.
# ============================================================

SUPABASE_URL=https://zpakjzbdqogjtpaqbcft.supabase.co
SUPABASE_ANON_KEY=sb_publishable_B-axGltWCEhj9Nn41RcnQg_viS0m3gd
